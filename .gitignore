# -----------------------------------------------------------------------------
# TERRAFORM ARTIFACTS
# -----------------------------------------------------------------------------

# Terraform State Files (Crucial to exclude - contains sensitive data and connection info)
# *.tfstate files store the current state of your deployed infrastructure.
# Never commit these to Git, especially if using local state management.
*.tfstate
*.tfstate.*.backup
*.tfstate.backup

# Local credentials (If stored in the working directory)
# .tfvars files often contain secrets, sensitive values, or configuration specific
# to a deployment environment.
# Note: It's better to manage secrets outside of version control (e.g., using Azure Key Vault or HashiCorp Vault).
# If you must use a .tfvars file, ensure it's specifically named and excluded.
# Example: 
# prod.tfvars
# credentials.tfvars
# You might want to INCLUDE a dummy 'terraform.tfvars' template for collaborators.
# To safely exclude files containing secrets, use the file name here:
# terraform.tfvars

# Plan output files
# These files contain the proposed changes to the infrastructure, including potential secrets.
*.tfplan
*.tfrun

# Logs and Crash Dumps
*.log
crash.log

# -----------------------------------------------------------------------------
# TERRAFORM CACHE & MODULES
# -----------------------------------------------------------------------------

# Terraform Module Cache
# This directory is where Terraform downloads the source code for external modules.
# It should always be excluded as it is environment-specific and regenerated by 'terraform init'.
.terraform/

# Terraform CLI configuration file
# This is typically local user configuration.
.terraformrc
terraform.rc

# -----------------------------------------------------------------------------
# COMMON IDE AND OS FILES (Optional, but good practice)
# -----------------------------------------------------------------------------
# Visual Studio Code
.vscode/

# MacOS files
.DS_Store

# Windows files
Thumbs.db

#terrafor lock files
.terraform.lock.hcl
terraform.tfvars
.terraform/
*.tfstate
